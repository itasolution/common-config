server {
    listen 80;
    server_name il_tuo_dominio.com;

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name il_tuo_dominio.com;

    # Altre direttive SSL come il percorso del certificato SSL e della chiave

    location / {
        # Le tue regole per gestire le richieste HTTPS
    }
}
